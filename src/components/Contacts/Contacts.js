import React from 'react'
import Layout from "../Layout";

const Contacts = () => {
    return (
        <Layout>
            <p>This will be contacts!</p>
        </Layout>
    )
};
export default Contacts;